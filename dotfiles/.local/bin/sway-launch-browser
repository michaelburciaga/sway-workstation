#!/usr/bin/env bash

set -u

for browser in brave-origin brave-browser firefox chromium chromium-browser; do
    if command -v "$browser" >/dev/null; then
        exec "$browser"
    fi
done

notify-send "Browser unavailable" \
    "Install Brave, Firefox, or Chromium to use the browser shortcut."
exit 1
